#include <stdio.h>

/**
 * main - C program that prints exactly a String, followed by a new line, using printf
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
